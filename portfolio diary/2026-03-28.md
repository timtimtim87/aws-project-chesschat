# 2026-03-28

## Feature Gap Closure Implementation
- Implemented section-ordered gap closure across app/backend/infra contracts.

### Completed
- Standardized room codes to 8-char alphanumeric across:
  - backend validation
  - frontend runtime checks
  - static-auth form validation
  - contract/docs references
- Added persistent app-wide authenticated websocket session in frontend (`AppSocketProvider`).
- Added ALB target group stickiness settings (`lb_cookie`, 86400s) and documented websocket scaling constraints.
- Added DynamoDB schema doc for social domains:
  - friendships
  - friend requests
  - challenges
  - notifications
- Added backend APIs/services for:
  - friends list + pending requests
  - request send/respond
  - challenges create/list/accept/get-by-id
  - notifications list/read
  - Chess.com link/unlink/status metadata on user records
- Added game protocol support:
  - host-authoritative `start_game` settings payload
  - unilateral validated `request_takeback`
  - `offer_draw` + `accept_draw` pending flow
- Extended game persistence to include:
  - UCI move list
  - SAN move list
  - FEN history for replay
- Updated history UI to include replay controls:
  - `View Board + Moves`
  - `Back` / `Forward`
  - stockfish eval placeholder
- Updated friends/profile chrome surfaces to use new APIs and settings/notification UX constraints.

### Validation
- Backend tests: pass (`npm --prefix app/backend test`).
- Frontend build: pass (`npm --prefix app/frontend run build`).
- Terraform validate blocked in local environment due provider plugin load failure.
- Existing preview-flow test mismatch still present in preview suite from prior UI copy changes.

## CC0 Chess Event Sounds (Implementation)
- Replaced synthetic room move beep with curated CC0 sound assets for gameplay events.

### Completed
- Added curated sound files under `app/frontend/public/sounds/chesschat/`:
  - `move.ogg`, `capture.ogg`, `castle.ogg`, `promotion.ogg`, `check.ogg`, `win.ogg`, `draw.ogg`, `error.ogg`.
- Added licensing/source record:
  - `app/frontend/public/sounds/chesschat/LICENSES.md`
  - Source: Kenney Interface Sounds (CC0), with pull date and per-file source mapping.
- Extended backend move classification contract:
  - `applyMove` now returns `moveType` (`move|capture|castle|promotion`) and `isCheck`.
  - `move_made` websocket payload now includes `moveType` and `isCheck`.
- Updated frontend room playback behavior:
  - `move_made`: mapped cue by move type with `check` priority.
  - `game_ended`: mapped to `win`/`draw`.
  - Blocking gameplay errors (e.g. `ILLEGAL_MOVE`) trigger `error` cue.
- Added persisted user preference:
  - `localStorage` key `chesschat_sound_enabled` (default `true`).
  - Real room UI toggle: `Sound On/Off`.
- Updated contract docs:
  - `DOCS/API_WS_CONTRACT.md` now explicitly documents `move_made.moveType` and `move_made.isCheck`.

### Validation
- Backend unit tests updated for move classification and `isCheck`.
- Frontend tests updated for sound mapping and disabled-preference suppression.
